/* A 5 digit number has been enterered through keyboard*/
//Sum of numbers with and without recursion

int* int_digit(int); 

int main ()
{
	int number;
	int * sol;
	registr int i;
	printf("Enter the number\n");
	scanf("%d",&number);
	
	for ( i=0;i<sizeof(int_digit(int));i++)
	{
		printf("%d",sol[i];
	}
}

int* int_digit(int)
{
			 
